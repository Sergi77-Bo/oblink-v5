databases:
  - name: oblink-db
    databaseName: oblink
    user: oblink_user
    plan: free

services:
  # Backend: Django Service
  - type: web
    name: oblink-backend
    runtime: python
    region: frankfurt
    plan: free
    buildCommand: "./setup_backend.sh"
    startCommand: "python backend/manage.py migrate && gunicorn config.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: oblink-db
          property: connectionString
      - key: FRONTEND_URL
        fromService:
          type: web
          name: oblink-frontend
          property: host

  # Frontend: React Static Site
  # Correct syntax: type: web, runtime: static
  - type: web
    name: oblink-frontend
    runtime: static
    plan: free
    repo: https://github.com/Sergi77-Bo/oblink-v5
    rootDir: frontend
    buildCommand: "npm install && npm run build"
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /**
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: oblink-backend
          property: host
