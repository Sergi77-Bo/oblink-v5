/** OBLINK Theme - Configuration requise **/

// Ajouter ces lignes AVANT la ligne "/* That's all, stop editing! */"

// ========== Supabase Configuration (REQUIRED) ==========
// Remplacer par votre vraie clé Supabase
define('SUPABASE_KEY', 'votre_clé_supabase_ici');

// ========== Debug Mode (Temporary - Remove when fixed) ==========
// Activer le debug pour voir les erreurs
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);

// Les erreurs seront loggées dans: /wp-content/debug.log


// ========== WordPress Debug Instructions ==========
/*
Si vous voyez toujours une erreur critique:

1. Allez dans le wp-content/debug.log pour voir les erreurs
2. Vérifiez que le thème oblink est bien installé en /wp-content/themes/oblink/
3. Assurez-vous que style.css existe: /wp-content/themes/oblink/style.css
4. Vérifiez les permissions des fichiers (755 pour les dossiers, 644 pour les fichiers)

Correction rapide si necessaire:
- Allez à Apparence -> Thèmes
- Activez un autre thème temporairement
- Réinstaller le thème oblink correctement
*/
